image: appleboy/node-less:latest
pages:
  stage: deploy
  script:
  script:
  - npm install -g @angular/cli
  - npm rebuild node-sass
  - npm install
  - ng build
  - mv dist/echoes-player/* public/
  artifacts:
    paths:
    - public
  only:
  - master
