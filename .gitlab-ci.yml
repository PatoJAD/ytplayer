image: node:latest
pages:
  stage: deploy
  script:
  script:
  - npm install -g @angular/cli
  - npm install node-sass
  - npm install
  - ng build
  - mv dist/echoes-player/* public/
  artifacts:
    paths:
    - public
  only:
  - master
